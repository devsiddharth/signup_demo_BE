server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: NewRoot@123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true